# Azure EDI File Processor - Environment Configuration Template
# Copy this file to .env and fill in your specific values

# =============================================================================
# AZURE SUBSCRIPTION & GENERAL SETTINGS
# =============================================================================

# Your Azure subscription ID (get it with: az account show --query id -o tsv)
AZURE_SUBSCRIPTION_ID= da8d4f5f-044d-4c3c-80cd-2d72f04f0b82

# Azure region/location for all resources (e.g., eastus, westus2, centralus)
LOCATION=eastus

# =============================================================================
# RESOURCE NAMING
# =============================================================================

# Resource group name (will be created if it doesn't exist)
RESOURCE_GROUP_NAME=rg-edi-processor-demo

# Storage account name (must be globally unique, 3-24 chars, lowercase letters and numbers only)
STORAGE_ACCOUNT_NAME=stedidemo$(date +%s | tail -c 6)

# Function app name (must be globally unique)
FUNCTION_APP_NAME=func-edi-processor-demo-$(date +%s | tail -c 6)

# SQL Server name (must be globally unique)
SQL_SERVER_NAME=sql-edi-demo-$(date +%s | tail -c 6)

# SQL Database name
SQL_DATABASE_NAME=EdiProcessorDB

# =============================================================================
# SQL SERVER AUTHENTICATION
# =============================================================================

# SQL Server admin username (cannot be 'admin', 'administrator', 'root', 'guest', etc.)
SQL_ADMIN_USERNAME=ediadmin

# SQL Server admin password (must be 8-128 chars, contain 3 of: uppercase, lowercase, numbers, symbols)
SQL_ADMIN_PASSWORD=YourSecureP@ssw0rd123!

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Set to 'true' to add your local IP to SQL Server firewall for testing (optional)
ALLOW_LOCAL_ACCESS=true

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. Make sure to replace all placeholder values above
# 2. Keep the $(date +%s | tail -c 6) parts to ensure unique naming
# 3. The SQL password must meet Azure complexity requirements
# 4. Storage account names must be globally unique across all Azure
# 5. Function app names must be globally unique across all Azure
# 6. SQL server names must be globally unique across all Azure
#
# After setting up these values, run: ./deploy-azure.sh
#